syntax = 'proto3';

package user;

service UserService {
   rpc getUser (getUserRequest) returns (getUserResponse);
}

message getUserRequest {
    string id = 1;
}

message getUserResponse {
    string id = 1;
    string name = 2;
    string email = 3;
    string role = 4;
    profilePic = 5;
}